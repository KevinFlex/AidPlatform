import react from 'react'

export default function Authentication() {
    return (
        <>
            <button className="login">Login</button>
            <button className="unlogin">Free Tour</button>
            <button className="logout">Logout</button>
        </>
    )
}